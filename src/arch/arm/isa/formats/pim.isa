
def format ArmPimInst() {{
    decode_block = '''
    {
        const uint8_t value = bits(machInst, 7, 0);
        const RegIndex rd = (RegIndex)(uint32_t)RD;
        const RegIndex rs = (RegIndex)(uint32_t)RS;
        return new PimMemset(machInst, rd, rs, value);
    }
    '''
}};
